CREATE TABLE nowphas(
    datetime INTEGER,
    inferiority INTEGER,
    mean_height REAL,
    mean_period REAL,
    significant_height REAL,
    significant_period REAL,
    ten_percent_height REAL,
    ten_percent_period REAL,
    max_height REAL,
    max_period REAL,
    direction INTEGER
)